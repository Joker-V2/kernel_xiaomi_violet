name: Build Kernel - ZyC Clang 22

on:
  workflow_dispatch:
  push:
    branches: [zycromerz]

env:
  KERNEL_NAME: "xcalibur-v5.4-violet-dynamic"
  KERNEL_VARIANT: "Retrofit Dynamic"
  KERNEL_SUPPORT: "Android 15.0-16.0"
  CONFIG_FILE: "vendor/violet-perf_defconfig"
  ARCH: "arm64"
  KBUILD_BUILD_HOST: "SuperiorOS"
  KBUILD_BUILD_USER: "Joker-V2"
  ANYKERNEL_REPO: "https://github.com/Joker-V2/AnyKernel3"
  ANYKERNEL_BRANCH: "sixteen"
  CLANG_URL: "https://github.com/ZyCromerZ/Clang/releases/download/22.0.0git-20250920-release/Clang-22.0.0git-20250920.tar.gz"
  ZIPSIGNER_URL: "https://github.com/Magisk-Modules-Repo/zipsigner/raw/master/bin/zipsigner-3.0-dexed.jar"

jobs:
  build:
    runs-on: ubuntu-22.04
    
    steps:
    - name: 📥 Checkout Kernel Source
      uses: actions/checkout@v4
      with:
        ref: zycromerz
        fetch-depth: 1
    
    - name: ⚙️ Setup Build Environment
      run: |
        sudo apt-get update -qq
        sudo apt-get install -y -qq \
          wget zip unzip ccache git-core gnupg flex bison build-essential \
          zip curl zlib1g-dev gcc-multilib g++-multilib libc6-dev-i386 \
          lib32ncurses5-dev x11proto-core-dev libx11-dev lib32z1-dev \
          libgl1-mesa-dev libxml2-utils xsltproc unzip fontconfig \
          openjdk-11-jdk
        
        # Set up environment variables
        echo "KERNEL_DIR=$(pwd)" >> $GITHUB_ENV
        echo "OBJ_DIR=$(pwd)/out" >> $GITHUB_ENV
        echo "ANYKERNEL_DIR=$(pwd)/anykernel" >> $GITHUB_ENV
        echo "TC_DIR=$(pwd)/tc" >> $GITHUB_ENV
        echo "CLANG_DIR=$(pwd)/tc" >> $GITHUB_ENV
        
        # Set up ccache
        echo "CCACHE_DIR=$(pwd)/.ccache" >> $GITHUB_ENV
        mkdir -p "$(pwd)/.ccache"
        ccache -M 10G
        
        # Display build info
        cores=$(nproc --all)
        os=$(cat /etc/issue | awk '{print $1, $2, $3}' | awk '{$1=$1};1' | sed 's/\\n//g')
        time=$(TZ="Asia/Kolkata" date "+%a %b %d %r")
        commit_head=$(git log --oneline -1)
        
        echo "Build Triggered ⌛"
        echo "Kernel: ${{ env.KERNEL_NAME }}"
        echo "Support: ${{ env.KERNEL_SUPPORT }}"
        echo "Variant: ${{ env.KERNEL_VARIANT }}"
        echo "Machine: ${os}"
        echo "Cores: ${cores}"
        echo "Time: ${time}"
        echo "Top Commit: ${commit_head}"
    
    - name: 🔧 Download Toolchain
      run: |
        if [ ! -d "${{ env.TC_DIR }}" ]; then
          echo "Toolchain not found! Cloning to ${{ env.TC_DIR }}..."
          echo "Toolchain not found! Cloning ZyC-Clang"
          wget -q "${{ env.CLANG_URL }}"
          mkdir -p "${{ env.TC_DIR }}"
          tar -xvf Clang-22.0.0git-20250920.tar.gz -C "${{ env.TC_DIR }}"
          rm -rf Clang-22.0.0git-20250920.tar.gz
        fi
        
        # Add toolchain to PATH
        echo "${{ env.CLANG_DIR }}/bin" >> $GITHUB_PATH
    
    - name: 🏗️ Build Kernel
      run: |
        START_TIME=$(date +"%s")
        
        # Clean previous builds
        rm -rf "${{ env.OBJ_DIR }}"
        mkdir -p "${{ env.OBJ_DIR }}"
        
        # Generate defconfig
        echo "########### Generating Defconfig ############"
        make -s ARCH="${{ env.ARCH }}" O="${{ env.OBJ_DIR }}" "${{ env.CONFIG_FILE }}" -j$(nproc --all)
        
        if [ $? -eq 0 ]; then
          echo "Defconfig generated successfully ✅"
        else
          echo "Defconfig generation failed ❌"
          exit 1
        fi
        
        # Compile kernel
        echo "######### Compiling kernel #########"
        make -j$(nproc --all) \
          O="${{ env.OBJ_DIR }}" \
          ARCH="${{ env.ARCH }}" \
          CC="ccache clang" \
          CLANG_TRIPLE="aarch64-linux-gnu-" \
          CROSS_COMPILE="aarch64-linux-gnu-" \
          CROSS_COMPILE_ARM32="arm-linux-gnueabi-" \
          LLVM=1 \
          LLVM_IAS=1 \
          2>&1 | tee error.log
        
        # Check if compilation succeeded
        COMPILED_IMAGE="${{ env.OBJ_DIR }}/arch/arm64/boot/Image.gz"
        COMPILED_DTBO="${{ env.OBJ_DIR }}/arch/arm64/boot/dtbo.img"
        
        if [[ -f "${COMPILED_IMAGE}" && -f "${COMPILED_DTBO}" ]]; then
          echo "Kernel compiled successfully ✅"
          END_TIME=$(date +"%s")
          DIFF=$((END_TIME - START_TIME))
          echo "Compilation took: $((DIFF / 60)) minute(s) and $((DIFF % 60)) second(s)"
        else
          echo "Compilation failed ❌"
          echo "Check error.log for details"
          exit 1
        fi
    
    - name: 📦 Package Kernel
      run: |
        START_TIME=$(date +"%s")
        ZIMAGE="${{ env.OBJ_DIR }}/arch/arm64/boot/Image.gz"
        DTBO="${{ env.OBJ_DIR }}/arch/arm64/boot/dtbo.img"
        ZIP_NAME="${{ env.KERNEL_NAME }}-$(date +"%d%m%Y-%H%M").zip"
        
        # Clone AnyKernel3
        rm -rf "${{ env.ANYKERNEL_DIR }}"
        git clone -q "${{ env.ANYKERNEL_REPO }}" -b "${{ env.ANYKERNEL_BRANCH }}" "${{ env.ANYKERNEL_DIR }}"
        
        # Copy kernel files
        cp -f "${ZIMAGE}" "${DTBO}" "${{ env.ANYKERNEL_DIR }}/"
        
        # Create zip
        cd "${{ env.ANYKERNEL_DIR }}"
        find . -name "*.zip" -type f -delete
        zip -r -9 AnyKernel.zip *
        mv AnyKernel.zip "${ZIP_NAME}"
        
        # Sign the zip
        echo "Signing build with AOSP keys"
        cd "${{ env.KERNEL_DIR }}"
        if [ ! -f zipsigner-3.0.jar ]; then
          wget -O zipsigner-3.0.jar "${{ env.ZIPSIGNER_URL }}"
        fi
        
        java -jar zipsigner-3.0.jar "${{ env.ANYKERNEL_DIR }}/${ZIP_NAME}" "${ZIP_NAME/.zip/-signed.zip}"
        SIGNED_ZIP="${ZIP_NAME/.zip/-signed.zip}"
        
        # Clean up
        rm -rf "${{ env.ANYKERNEL_DIR }}"
        
        END_TIME=$(date +"%s")
        DIFF=$((END_TIME - START_TIME))
        
        echo "############################################"
        echo "Build took: $((DIFF / 60)) minute(s) and $((DIFF % 60)) second(s)"
        echo "Compiled & Signed successfully ✅"
        echo "Build file: ${SIGNED_ZIP}"
        echo "############################################"
        echo "############# OkThisIsEpic! ###############"
        echo "############################################"
    
    - name: 📤 Upload Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: kernel-build
        path: |
          ${{ env.KERNEL_DIR }}/*.zip
          ${{ env.KERNEL_DIR }}/error.log
          ${{ env.OBJ_DIR }}/arch/arm64/boot/
        retention-days: 30
    
    # إضافة الكاش لتحسين الأداء
    - name: 💾 Cache Toolchain
      uses: actions/cache@v4
      with:
        path: |
          ${{ env.TC_DIR }}
          ${{ env.CCACHE_DIR }}
        key: ${{ runner.os }}-tc-${{ hashFiles('**/Kbuild', '**/Makefile') }}
        restore-keys: |
          ${{ runner.os }}-tc-
